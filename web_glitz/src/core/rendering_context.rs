use core::gpu_command::{CommandObject, GpuCommand};

pub struct RenderingContext {

}

impl RenderingContext {
//    pub fn execute<T, E>(&mut self, command: T) -> Result<E::Output, E::Error> where T: Into<GpuCommand<E>>, E: GpuExecutable {
//        command.into().execute(self)
//    }
}
